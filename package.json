{
  "name": "viewlog",
  "version": "1.0.0",
  "private": true,
  "description": "App simple pour analyser des fichiers de logs (drag & drop, dashboard, filtres).",
  "scripts": {
    "build": "docker compose build",
    "start": "docker compose up",
    "start:detached": "docker compose up -d",
    "stop": "docker compose down",
    "dev:server": "cd server && npm install && npm run dev",
    "dev:web": "cd web && npm install && npm run dev"
  }
}
